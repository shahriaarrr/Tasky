name: CI / CD Pipeline

on:
  push:
    branches:
      - main
      - dev
    tags:
      - "v*.*.*"
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, windows, darwin]
        goarch: [amd64]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build binary for ${{ matrix.goos }}/${{ matrix.goarch }}
        run: |
          mkdir -p build
          BIN=tasky
          EXT=""
          if [ "${{ matrix.goos }}" = "windows" ]; then
            EXT=".exe"
          fi
          env GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} CGO_ENABLED=0 \
            go build -trimpath -ldflags="-s -w" -o build/${BIN}_${{ matrix.goos }}_${{ matrix.goarch }}${EXT} ./cli/tasky

      - name: Compress binary
        run: |
          cd build
          BIN=tasky
          FILE="${BIN}_${{ matrix.goos }}_${{ matrix.goarch }}"
          if [ "${{ matrix.goos }}" = "windows" ]; then
            FILE="${FILE}.exe"
            zip -9 "${FILE}.zip" "${FILE}"
            rm "${FILE}"
          else
            tar czf "${FILE}.tar.gz" "${FILE}"
            rm "${FILE}"
          fi

      - name: List files before upload
        run: ls -lh build

      - name: Upload build artifacts
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            build/*.zip
            build/*.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: build

      - name: Generate combined SHA256 checksum
        run: |
          cd build
          sha256sum *.{zip,tar.gz} > SHA256SUMS

      - name: List build directory
        run: ls -lh build

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: build/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
